/** components/features/heatmap-calendar.tsx — stub for Phase 4 */
export function HeatmapCalendar({ habitId }: { habitId: string }) {
  return (
    <div className="rounded-lg border bg-card p-4">
      <p className="font-body text-sm text-muted-foreground">
        Heatmap for {habitId} — GSAP stagger animation in Phase 4
      </p>
    </div>
  );
}
